# movie-recommendation-system-data-engineering-pipeline
End-to-end data engineering pipeline for a movie recommendation system using the MovieLens dataset. The pipeline performs data ingestion with AWS S3, ETL processing with Python, and loads the transformed data into PostgreSQL for analytics and machine learning applications.
